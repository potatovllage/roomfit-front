# Development backend endpoint. Do not include a trailing slash.
VITE_ROOMFIT_API_BASE_URL=http://43.203.208.230:8000

# Development-only bearer token. In production, obtain this token through the
# app's authentication flow or proxy API requests through a backend-for-frontend.
VITE_ROOMFIT_API_TOKEN=
